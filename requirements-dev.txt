# Format validation only: Python 3.11+ and Git are required to install this tooling.
# The writing skill has no runtime dependencies; the optional helper uses Python 3.10+.
# Use the official reference source, pinned to a reviewed commit, rather than a PyPI namesake.
skills-ref @ git+https://github.com/agentskills/agentskills.git@69ef37e9424c0a7ea9dd2293b559e43ec8176379#subdirectory=skills-ref
# Pin the reference validator's runtime dependencies, including transitive dependencies.
click==8.5.0
strictyaml==1.7.3
python-dateutil==2.9.0.post0
six==1.17.0
